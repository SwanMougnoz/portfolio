import React, {Component} from 'react'
import {Grid} from "semantic-ui-react";

export default class LoadingProgress extends Component {
  render() {
    return (
      <Grid.Row>The loading bar</Grid.Row>
    )
  }
}